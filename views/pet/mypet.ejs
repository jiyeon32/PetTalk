<!DOCTYPE html>
<html lang="ko">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1">
<<<<<<< HEAD
  <title>반려동물 커뮤니티</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="fontawesome/css/all.min.css">
  <link href="https://fonts.googleapis.com/css2?family=Source+Sans+Pro&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="css/mypet.css">
</head>

<body>

  <header class="header-menu" id="header-menu">
    <div class="header-menu-wrapper">
      <nav class="navbar navbar-expand-lg bg-body-tertiary">
        <div class="container-fluid">
          <a class="navbar-brand">PetTalk</a>
          <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item"><a class="nav-link active" href="/">홈</a></li>
            <li class="nav-item"><a class="nav-link" href="/profile">마이페이지</a></li>
            <li class="nav-item"><a class="nav-link" href="/posts">게시판</a></li>
            <li class="nav-item"><a class="nav-link" href="/mypet">마이펫</a></li>
            <li class="nav-item"><a class="nav-link" href="/clinic">병원찾기</a></li>
          </ul>
          <form class="d-flex">
            <button type="button" onclick="window.location.href='/register';">회원가입</button>
            <button type="button" onclick="window.location.href='/login';">로그인</button>
          </form>
        </div>
      </nav>
    </div>
  </header>

  <div class="mypage-header">
    <h2>반려동물 등록</h2>
  </div>

  <div class="mypet-info text-center">
    <div class="image-upload mb-4">
      <form class="inputImg" method="post" enctype="multipart/form-data">
        <div class="addImg" id="img-show">
          <!-- 이미지 미리보기 영역 -->
        </div>
        <input type="file" accept="image/*" onchange="loadFile(event)">
      </form>
    </div>

    <form class="d-inline-block text-start" style="width: 100%;">
      <div class="row g-3 align-items-center">
        <div class="col-md-3 text-end">
          <label for="inputPname" class="col-form-label">이름</label>
        </div>
        <div class="col-md-6">
          <input type="text" id="inputPname" class="form-control">
        </div>
      </div>

      <div class="row g-3 align-items-center">
        <div class="col-md-3 text-end">
          <label for="inputGender" class="col-form-label">성별</label>
        </div>
        <div class="col-md-6">
          <select class="form-select" id="inputGender">
            <option selected>성별</option>
            <option value="1">여자</option>
            <option value="2">남자</option>
          </select>
        </div>
      </div>

      <div class="row g-3 align-items-center">
        <div class="col-md-3 text-end">
          <label for="inputPweight" class="col-form-label">몸무게</label>
        </div>
        <div class="col-md-6">
          <input type="text" id="inputPweight" class="form-control">
        </div>
      </div>

      <div class="row g-3 align-items-center">
        <div class="col-md-3 text-end">
          <label for="inputPsik" class="col-form-label">질환</label>
        </div>
        <div class="col-md-6">
          <input type="text" id="inputPsik" class="form-control">
        </div>
      </div>

      <div class="row g-3 align-items-center">
        <div class="col-md-3 text-end">
          <label for="inputPvaccin" class="col-form-label">예방접종</label>
        </div>
        <div class="col-md-6">
          <input type="date" id="inputPvaccin" class="form-control">
        </div>
      </div>

      <button type="submit" class="btn btn-primary mt-4">등록하기</button>
=======
  <title>반려동물 관리</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
  <link href="https://fonts.googleapis.com/css2?family=Source+Sans+Pro&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="css/common.css">
  <link rel="stylesheet" href="css/mypet.css">
</head>
<body>

<!-- 공통 헤더 포함 -->
<%- include('../header') %>
  
  <header class="text-center my-4">
    <h2>반려동물 관리</h2>
  </header>

  <!-- 반려동물 정보 -->
  <div class="container">
    <% if (pets && pets.length > 0) { %>
      <div id="pet-info">
        <% pets.forEach(pet => { %>
          <div class="card mb-4">
            <% if (pet.photo) { %>
              <img src="<%= pet.photo %>" class="card-img-top" alt="<%= pet.name %> 사진">
            <% } else { %>
              <div class="card-img-top bg-secondary text-white d-flex align-items-center justify-content-center" style="height: 200px;">사진 없음</div>
            <% } %>
            <div class="card-body">
              <h5 class="card-title text-center"><%= pet.name %></h5>
              <p class="card-text">성별: <%= pet.gender === 'Male' ? '남' : '여' %></p>
              <p class="card-text">몸무게: 
                <% if (pet.weight && pet.weight.length > 0) { %>
                  <%= pet.weight[pet.weight.length - 1].value %> kg
                <% } else { %>
                  정보 없음
                <% } %>
              </p>
              <p class="card-text">질환: <%= pet.diseases || '없음' %></p>
              <p class="card-text">나이: <%= pet.age || '정보 없음' %> 세</p>
              <div class="button-group">
                <button class="btn btn-primary btn-sm" onclick="editPet()">수정</button>
                <form action="/mypet/<%= pet._id %>?_method=DELETE" method="POST">
                  <button class="btn btn-danger btn-sm" type="submit">삭제</button>
                </form>
              </div>
            </div>
          </div>
        <% }) %>
      </div>
    <% } else { %>
      <p class="text-center text-muted">등록된 반려동물이 없습니다. 아래 폼을 통해 등록해주세요.</p>
    <% } %>
  </div>

  <!-- 반려동물 등록/수정 입력창 -->
  <div class="container mt-4" id="pet-form" style="<% if (pets && pets.length > 0) { %>display: none;<% } %>">
    <form action="<% if (pets && pets.length > 0) { %>/mypet/<%= pets[0]._id %>?_method=PUT<% } else { %>/mypet<% } %>" method="POST" enctype="multipart/form-data">
      <div class="mb-3">
        <label for="photo" class="form-label">사진 업로드</label>
        <input type="file" name="photo" class="form-control" accept="image/*" onchange="loadFile(event)">
        <div class="addImg">
          <img id="output" width="200" height="150" style="object-fit: cover; display: none;">
        </div>
      </div>

      <input type="text" name="name" placeholder="이름" value="<%= pets && pets.length > 0 ? pets[0].name : '' %>" required>
      <select name="gender" required>
        <option value="Male" <% if (pets && pets.length > 0 && pets[0].gender === 'Male') { %>selected<% } %>>남자</option>
        <option value="Female" <% if (pets && pets.length > 0 && pets[0].gender === 'Female') { %>selected<% } %>>여자</option>
      </select>
      <input type="number" name="weight" placeholder="몸무게 (kg)" value="<%= pets && pets.length > 0 ? pets[0].weight[0]?.value : '' %>" required>
      <input type="text" name="diseases" placeholder="질환" value="<%= pets && pets.length > 0 ? pets[0].diseases : '' %>">
      <input type="number" name="age" placeholder="나이 (세)" value="<%= pets && pets.length > 0 ? pets[0].age : '' %>" required>
      <button type="submit" class="btn btn-success"><% if (pets && pets.length > 0) { %>저장하기<% } else { %>등록하기<% } %></button>
      <% if (pets && pets.length > 0) { %>
        <button type="button" class="btn btn-secondary" onclick="cancelEdit()">취소</button>
      <% } %>
>>>>>>> 4f0fc6b (final)
    </form>
  </div>

  <script>
    function loadFile(event) {
<<<<<<< HEAD
      var image = document.getElementById('img-show');
      image.innerHTML = ''; // 기존 이미지 제거
      var imgElement = document.createElement('img');
      imgElement.src = URL.createObjectURL(event.target.files[0]);
      imgElement.style.width = '100%'; // 이미지 너비를 컨테이너에 맞추기
      imgElement.style.height = '100%'; // 이미지 높이를 컨테이너에 맞추기
      imgElement.style.objectFit = 'cover'; // 이미지 자르기
      image.appendChild(imgElement);
=======
      const output = document.getElementById('output');
      output.src = URL.createObjectURL(event.target.files[0]);
      output.style.display = 'block';
    }

    function editPet() {
      document.getElementById('pet-info').style.display = 'none';
      document.getElementById('pet-form').style.display = 'block';
    }

    function cancelEdit() {
      document.getElementById('pet-info').style.display = 'block';
      document.getElementById('pet-form').style.display = 'none';
>>>>>>> 4f0fc6b (final)
    }
  </script>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
<<<<<<< HEAD
  <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.8/dist/umd/popper.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.min.js"></script>
=======
>>>>>>> 4f0fc6b (final)
</body>

</html>
